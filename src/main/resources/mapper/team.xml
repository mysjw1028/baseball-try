<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.team.TeamDao">

	<insert id="insert">
		INSERT INTO team(NAME, created_at) VALUES(#{name},NOW());
	</insert>

	<select id="findAll"
		resultType="site.metacoding.red.domain.team.Team">
		SELECT t.id, s.name stadium_name, row_number() over(order By id asc) as 
		NO, t.NAME, t.created_at
		FROM team t
		INNER JOIN stadium s
		ON t.stadium_id = s.id
	</select>

	<select id="findById"
		resultType="site.metacoding.red.domain.team.Team">
	</select>

	<update id="update">
	</update>


	<insert id="deleteById">
	</insert>


</mapper>